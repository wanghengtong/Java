<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wanghengtong.threadpool.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.wanghengtong.threadpool.entity.UserDO">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="age" jdbcType="INTEGER" property="age"/>
        <result column="mobile" jdbcType="VARCHAR" property="mobile"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
    </resultMap>

    <insert id="batchInsert">
        replace into t_user (id, name, age, mobile, email)
        values
        <foreach collection="list" item="user" separator=",">
            (#{user.id,jdbcType=INTEGER}, #{user.name,jdbcType=VARCHAR},
            #{user.age,jdbcType=INTEGER}, #{user.mobile,jdbcType=VARCHAR}, #{user.email,jdbcType=VARCHAR})
        </foreach>
    </insert>

</mapper>