server:
  port: 8080

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password: xxx
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 500
        min-idle: 0
    lettuce:
      shutdown-timeout: 0